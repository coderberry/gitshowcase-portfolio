run.config:

  engine: ruby

  engine.config:
    runtime: ruby-2.4.0

# add extra packages
  extra_packages:
    - nodejs

# add a database
data.db:
  image: nanobox/postgresql:9.5

data.redis:
  image: nanobox/redis:3.0